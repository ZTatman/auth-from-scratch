{
  "name": "auth-login-page",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "concurrently -n \"server,client\" -c \"blue,green\" \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "concurrently -n \"server,client\" -c \"blue,green\" \"cd server && npm run build\" \"cd client && npm run build\"",
    "format": "cd server && npm run format && cd client && npm run format"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.15",
    "tailwindcss": "^4.1.15"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.7.4",
    "prettier-plugin-tailwindcss": "^0.7.2"
  }
}
